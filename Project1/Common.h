#pragma once
#include <vector> // 他のファイルで <vector> を使う可能性があるため、念のためインクルード

/*
 * Common.h
 * 役割: プロジェクト全体で共有する定数を定義します。ウィンドウサイズ、数学定数、
 *       レンダリング関連のパラメータなどを一元管理します。
 *
 * 最初のコードからの変更点・コメント:
 * - 匿名名前空間 (`namespace {}`) を使用しています。
 *   これにより、ここで定義された定数は各cppファイル内で内部リンケージを持ちます。
 *   アクセス時に名前空間修飾子は不要です（例: `PI` と直接記述）。
 * - `static const` から `const` に変更（匿名名前空間内では `const` だけで内部リンケージになるため）。
 * - `PI` の値をより精度の高いfloat値に変更しました。
 * - `SCREEN` 定数の意味（射影平面までの距離）についてコメントを追加しました。
 */

namespace // 匿名名前空間: ここで定義された定数はファイルスコープ（内部リンケージ）を持つ
{
    // 数学定数
    const float PI = 3.1415926535f;       // 円周率 (float)
    const float ONE_DEGREE = PI / 180.0f; // 1度をラジアンに変換する係数

    // ウィンドウ設定
    const float WINDOW_WIDTH = 800.f;     // ウィンドウの幅
    const float WINDOW_HEIGHT = 600.f;    // ウィンドウの高さ

    // カメラ・レンダリング設定
    const float SCREEN = 500.f;           // 射影平面 (仮想スクリーン) までの距離
    // この値が透視投影の強さに影響します。
}